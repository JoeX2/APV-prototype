<%= form_for(@data_letter) do |f| %>
  <% if @data_letter.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@data_letter.errors.count, "error") %> prohibited this data_letter from being saved:</h2>

      <ul>
      <% @data_letter.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :titel %><br>
    <%= f.text_field :titel %>
  </div>
  <div class="field">
    <%= f.label :text %><br>
    <%= f.text_area :text %>
  </div>
  <div class="field">
    <%= f.label :material_id %><br>
    <%= f.text_field :material_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
